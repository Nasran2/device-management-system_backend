<x-layouts.app :title="ucfirst($type).' Report'"><div class="mb-7 flex flex-wrap items-end justify-between gap-3"><div><p class="eyebrow">Tenant-scoped reports</p><h1 class="page-title">{{ ucfirst($type) }} Report</h1></div><div class="flex gap-2"><a class="secondary-button" href="{{ request()->fullUrlWithQuery(['format'=>'csv']) }}">Excel / CSV</a><button class="secondary-button" onclick="window.print()">Print / Save PDF</button></div></div><nav class="mb-5 flex flex-wrap gap-2">@foreach(['shops','customers','devices','installments','payments','outstanding','overdue','commissions','settlements','sms','setup'] as $report)<a class="{{ $type===$report?'primary-button':'secondary-button' }}" href="{{ route('reports.business',$report) }}">{{ ucfirst($report) }}</a>@endforeach</nav><section class="panel"><form class="panel-header flex-wrap" method="get"><input class="field-input max-w-48" type="date" name="date_from" value="{{ request('date_from') }}"><input class="field-input max-w-48" type="date" name="date_to" value="{{ request('date_to') }}"><button class="secondary-button">Filter</button></form><div class="overflow-x-auto"><table class="data-table"><thead><tr>@foreach($headers as $h)<th>{{ $h }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($headers) }}" class="py-10 text-center">No matching records.</td></tr>@endforelse</tbody></table></div></section></x-layouts.app>
