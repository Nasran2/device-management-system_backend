<x-layouts.app title="Devices">
    <div class="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">Inventory</p><h1 class="page-title">{{ auth()->user()->isSuperAdmin() ? 'All devices' : 'My devices' }}</h1><p class="page-copy">Search registered phones and review confirmed management state.</p></div><a class="primary-button" href="{{ route('devices.create') }}">+ Register device</a></div>
    <section class="panel"><form class="panel-header flex-wrap" method="get"><input class="field-input max-w-md" name="search" value="{{ request('search') }}" placeholder="Search customer, brand, or model"><select class="field-input max-w-52" name="status"><option value="">All statuses</option>@foreach(['pending_activation','active_unlocked','locked','offline','permanently_released'] as $status)<option @selected(request('status')===$status) value="{{ $status }}">{{ ucwords(str_replace('_',' ',$status)) }}</option>@endforeach</select><button class="secondary-button">Filter</button></form>
        <div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Customer</th><th>Device</th><th>IMEI</th><th>Selling price</th><th>Added by</th><th>Mode</th><th>Status</th><th></th></tr></thead><tbody>
        @forelse($devices as $device)<tr><td><p class="font-semibold">{{ $device->customer->name }}</p><p class="text-xs text-slate-500">{{ $device->customer->phone }}</p></td><td>{{ $device->brand }} {{ $device->model }}</td><td class="font-mono text-xs">{{ $device->masked_imei }}</td><td>{{ number_format($device->selling_price,2) }} {{ $device->currency }}</td><td>{{ $device->admin->name }}</td><td class="capitalize">{{ $device->management_mode }}</td><td><span class="status-pill {{ $device->lock_status === 'locked' ? 'bg-red-50 text-red-700' : 'bg-slate-100 text-slate-700' }}">{{ str_replace('_',' ',$device->status) }}</span></td><td><a class="font-semibold text-indigo-600" href="{{ route('devices.show',$device) }}">View</a></td></tr>@empty<tr><td colspan="8" class="py-12 text-center text-slate-500">No matching devices.</td></tr>@endforelse
        </tbody></table></div><div class="p-5">{{ $devices->links() }}</div>
    </section>
</x-layouts.app>
