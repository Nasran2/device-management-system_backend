<x-layouts.app title="Full Management QR"><div class="mx-auto max-w-5xl"><p class="eyebrow">Zero-touch shop enrollment</p><h1 class="page-title">Full Management QR</h1><div class="mt-6 grid gap-6 lg:grid-cols-2"><section class="panel p-6"><img class="mx-auto w-full max-w-[650px]" src="{{ route('devices.provisioning.image',[$device,$token]) }}" alt="Device Owner provisioning QR"><p class="mt-3 text-center font-semibold">Expires <span id="expiry">{{ $token->expires_at->toIso8601String() }}</span></p><div class="mt-4 flex flex-wrap gap-2"><button class="primary-button" onclick="print()">Print QR</button><a class="secondary-button" href="{{ route('devices.provisioning.image',[$device,$token,'download'=>1]) }}">Download PNG</a><form method="post" action="{{ route('devices.provisioning.revoke',[$device,$token]) }}">@csrf<button class="danger-button">Revoke QR</button></form></div></section><section class="space-y-5"><div class="panel p-6"><h2 class="section-title">{{ $device->customer->name }}</h2><dl class="details-grid mt-4"><div><dt>Device</dt><dd>{{ $device->brand }} {{ $device->model }}</dd></div><div><dt>IMEI</dt><dd>{{ $device->masked_imei }}</dd></div><div><dt>Reference</dt><dd>{{ $device->uuid }}</dd></div><div><dt>Price</dt><dd>{{ $device->selling_price }} {{ $device->currency }}</dd></div><div><dt>Status</dt><dd>{{ $token->status }}</dd></div></dl></div><div class="rounded-2xl bg-amber-50 p-4 text-amber-900">This is a Device Owner provisioning QR. It only works during Android initial setup on a supported new or factory-reset phone.</div><div class="panel p-6"><h2 class="section-title">Shop instructions</h2><ol class="mt-4 list-decimal space-y-2 pl-5"><li>Use a new or factory-reset Android phone.</li><li>Stop at the first Android welcome screen.</li><li>Tap the same empty place six times.</li><li>Connect to Wi-Fi.</li><li>Scan this QR code.</li><li>Wait while DeviceGuard downloads.</li><li>Enter the four-digit Management PIN.</li><li>Wait until the dashboard shows Managed Mode.</li></ol></div></section></div></div></x-layouts.app>
