<x-layouts.app title="Archived Devices"><div class="mb-7"><p class="eyebrow">Super Admin</p><h1 class="page-title">Archived Devices</h1><p class="page-copy">Archived records are hidden from normal device lists and cannot receive new commands.</p></div><section class="panel"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Customer</th><th>Device</th><th>IMEI</th><th>Price</th><th>Added by</th><th>Previous status</th><th>Archived by/date</th><th>Actions</th></tr></thead><tbody>@forelse($devices as $device)<tr><td>{{ $device->customer->name }}</td><td>{{ $device->brand }} {{ $device->model }}</td><td>{{ $device->masked_imei }}</td><td>{{ number_format($device->selling_price,2) }} {{ $device->currency }}</td><td>{{ $device->admin->name }}</td><td>{{ str_replace('_',' ',$device->status_before_archive ?: $device->status) }}</td><td>{{ $device->archivedBy?->name ?? '—' }}<br><small>{{ $device->deleted_at?->format('Y-m-d H:i') }}</small></td><td><details><summary class="cursor-pointer font-semibold">Actions</summary><div class="mt-3 w-72 space-y-3"><form method="post" action="{{ route('devices.restore',$device->id) }}">@csrf<label class="field-label">Password<input class="field-input" type="password" name="password" required></label><button class="primary-button w-full">Restore</button></form><form method="post" action="{{ route('devices.destroy',$device->id) }}" class="rounded-xl border border-red-200 p-3">@csrf @method('delete')<p class="font-bold text-red-700">Permanently Delete</p><label class="field-label">Reason<textarea class="field-input" name="reason" required></textarea></label><label class="field-label">Password<input class="field-input" type="password" name="password" required></label><label class="field-label">Type DELETE<input class="field-input" name="confirmation" pattern="DELETE" required></label><label class="flex gap-2 text-sm"><input type="checkbox" name="confirmed" value="1" required> I understand this permanently removes the record.</label><button class="danger-button mt-2 w-full">Permanently Delete</button></form></div></details></td></tr>@empty<tr><td colspan="8" class="py-10 text-center">No archived devices.</td></tr>@endforelse</tbody></table></div><div class="p-5">{{ $devices->links() }}</div></section></x-layouts.app>
